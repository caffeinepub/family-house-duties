{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Swap tab hero/header images with modern static banners",
  "requirements": [
    {
      "id": "REQ-1",
      "summary": "Replace the hero/header banner images for Tasks, Dinner Rota, Calendar, and People with clean modern visuals while keeping the exact same referenced filenames and dimensions.",
      "acceptanceCriteria": [
        "The hero/header images for Tasks, Dinner Rota, Calendar, and People appear updated in the UI without layout changes.",
        "The image paths referenced by the UI remain unchanged (same filenames) so no broken images occur.",
        "Images feel clean/modern (natural light, minimal clutter, contemporary look) and remain readable behind the existing gradient overlay."
      ],
      "file_operations": [
        {
          "path": "frontend/public/assets/generated/header-tasks.dim_1600x420.jpg",
          "operation": "modify",
          "description": "Replace the binary image content with a clean, modern household task-organization banner while preserving the exact filename and 1600x420 dimensions."
        },
        {
          "path": "frontend/public/assets/generated/header-dinner.dim_1600x420.jpg",
          "operation": "modify",
          "description": "Replace the binary image content with a clean, modern family dinner prep/cooking banner while preserving the exact filename and 1600x420 dimensions."
        },
        {
          "path": "frontend/public/assets/generated/header-calendar.dim_1600x420.jpg",
          "operation": "modify",
          "description": "Replace the binary image content with a clean, modern calendar/planning banner while preserving the exact filename and 1600x420 dimensions."
        },
        {
          "path": "frontend/public/assets/generated/header-people.dim_1600x420.jpg",
          "operation": "modify",
          "description": "Replace the binary image content with a clean, modern people/community-themed banner while preserving the exact filename and 1600x420 dimensions."
        }
      ]
    },
    {
      "id": "REQ-2",
      "summary": "Ensure the updated hero/header images exist as static assets under frontend/public/assets/generated with the exact filenames used by each tab’s HeroHeader.",
      "acceptanceCriteria": [
        "The following assets exist in frontend/public/assets/generated and load successfully in the browser: header-tasks.dim_1600x420.jpg, header-dinner.dim_1600x420.jpg, header-calendar.dim_1600x420.jpg, header-people.dim_1600x420.jpg.",
        "No console 404s occur for the header image assets when navigating across all tabs."
      ],
      "file_operations": [
        {
          "path": "frontend/public/assets/generated/header-tasks.dim_1600x420.jpg",
          "operation": "create",
          "description": "Add the updated Tasks header banner asset at this exact path/filename (if already present, keep the same filename and overwrite content with the new image)."
        },
        {
          "path": "frontend/public/assets/generated/header-dinner.dim_1600x420.jpg",
          "operation": "create",
          "description": "Add the updated Dinner Rota header banner asset at this exact path/filename (if already present, keep the same filename and overwrite content with the new image)."
        },
        {
          "path": "frontend/public/assets/generated/header-calendar.dim_1600x420.jpg",
          "operation": "create",
          "description": "Add the updated Calendar header banner asset at this exact path/filename (if already present, keep the same filename and overwrite content with the new image)."
        },
        {
          "path": "frontend/public/assets/generated/header-people.dim_1600x420.jpg",
          "operation": "create",
          "description": "Add the updated People header banner asset at this exact path/filename (if already present, keep the same filename and overwrite content with the new image)."
        }
      ]
    }
  ]
}